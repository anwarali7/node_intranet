export default function UserEdit(req, res) {
  // Get userid or email from :id
  const userId = "something";

  // query database, handle errors

  // send ok message
  res.json({message: "User deleted."});
}
